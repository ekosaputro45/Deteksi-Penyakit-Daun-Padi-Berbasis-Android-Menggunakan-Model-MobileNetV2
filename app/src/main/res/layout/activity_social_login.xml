<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:padding="24dp"
    tools:context=".SocialLoginActivity">

    <!-- Anda perlu menambahkan gambar ilustrasi Anda sendiri di sini -->

    <ImageView
        android:id="@+id/ivIllustration"
        android:layout_width="387dp"
        android:layout_height="321dp"
        android:layout_marginTop="50dp"
        android:src="@drawable/padi_gabah"
        tools:srcCompat="@drawable/padi_gabah" />

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivIllustration"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:text="Masuk sekarang"
        android:textColor="@android:color/black"
        android:textSize="40sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnContinueWithPassword"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="80dp"
        android:backgroundTint="#5865F2"
        android:text="Masuk"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:cornerRadius="28dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="32dp"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="tidak punya akun? "
            android:textColor="@android:color/darker_gray" />

        <TextView
            android:id="@+id/tvSignUp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Daftar"
            android:textColor="#5865F2"
            android:textStyle="bold" />
    </LinearLayout>

</RelativeLayout>
